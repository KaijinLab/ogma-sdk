## What changed

<!-- Describe the type change: new interface, corrected return type, added method, etc. -->

## Why

<!-- Link to the Ogma source or runtime behavior that justifies this change. -->

## Checklist

- [ ] Types match the actual Ogma runtime behavior
- [ ] No breaking changes to existing interfaces (or version bump noted)
- [ ] `index.d.ts` compiles without errors (`npx tsc --noEmit index.d.ts --strict`)
